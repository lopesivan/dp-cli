// Gerado com: prefix=app, adt_name=Customer, allocation_strategy=dynamic
#include "app_Customer.h"

int main() {
  app_CustomerPtr customer = app_Customer_create(/* params */);
  // Use customer...
  app_Customer_destroy(customer);
  return 0;
}
